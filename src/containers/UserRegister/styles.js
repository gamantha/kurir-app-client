import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1
    },
    inputText: {
        marginBottom: 10,
        borderColor: '#FFBBEE',
        borderWidth: 1
    }
});
